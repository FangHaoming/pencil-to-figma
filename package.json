{
  "name": "pencil-to-figma",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && vite build --config vite.plugin.config.ts && vite build --config vite.ui.config.ts",
    "dev": "vite build --watch --config vite.plugin.config.ts & vite build --watch --config vite.ui.config.ts",
    "test": "tsx --test tests/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.124.0",
    "@sveltejs/vite-plugin-svelte": "^5.1.1",
    "@types/node": "^25.6.0",
    "svelte": "^5.55.4",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3",
    "vite": "^6.4.2"
  }
}
